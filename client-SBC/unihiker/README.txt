use python3 START.py to get started, or set up in the unihiker to run that python3 script in the menu

You may well have to install somelibraries for Python, such as:

pip3 install ifaddr
pip3 install pygeohash
pip3 install websockets

If you are planning to use the GPS module:
pip3 install pyubx2  

